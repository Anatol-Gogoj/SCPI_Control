python-usbtmc==0.8
pyusb==1.3.1
PyVISA==1.16.1
PyVISA-py==0.8.1
typing_extensions==4.15.0

# Webcam tab (live preview + interval/stepped capture). cv2 captures frames,
# Pillow displays them in Tk, numpy backs the focus metric. The Webcam tab
# degrades gracefully (shows an install hint) if these are absent.
opencv-python-headless==4.13.0.92
Pillow==12.2.0
numpy==2.4.6

# Battery Data tab (issue #31: cycler-export processing, adapted from
# Pingwinos40/BatteryProcessing). The tab degrades to an install hint
# if these are absent.
pandas>=1.3
matplotlib>=3.5
openpyxl>=3.0
